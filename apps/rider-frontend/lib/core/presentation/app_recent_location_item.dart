import 'package:flutter/material.dart';
import 'package:generic_map/interfaces/place.dart';

class AppRecentLocationItem extends StatelessWidget {
  final Place place;

  const AppRecentLocationItem({
    super.key,
    required this.place,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
